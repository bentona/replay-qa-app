
.col-md-4.col-md-offset-4
  %h1 User Profile
  - @user.attributes.each do |property, value|
    -if property != 'password' && property != 'password_digest' && property != 'created_at' && property != 'updated_at'
      =label_tag property.capitalize
      =text_field_tag value,  value, class: 'form-control'

= link_to 'Edit', edit_user_path(@user)
\|
= link_to 'Back', users_path
